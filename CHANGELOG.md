# 변경 이력

## 1.8.6

- 입력표의 `상세`를 운영체제 버튼 스타일과 무관한 셀 직접 그리기 방식으로 교체해 글자 잘림을 방지했습니다.
- `상세` 셀을 캡슐형으로 더 둥글게 만들고 기본값과 다른 상세조건이 있는 행은 진한 남색으로 구분합니다.
- 보조입력 머리글을 `교차로/서비스수준/(참고)`, `접근로/서비스수준/(참고)`의 3줄로 줄였습니다.

## 1.8.5

- Excel 연동 자동 새로고침과 체크박스 이동을 분리해 10개 이상의 구간도 안정적으로 동시에 선택되도록 수정했습니다.
- 자동 새로고침 뒤에도 현재 탭에서 체크한 구간 선택이 유지되도록 보강했습니다.
- 데이터 행 높이를 유지하면서 `상세` 버튼의 파란 면적과 글자 여백을 정상 크기로 복원했습니다.
- 차로수·평균통행속도와 보조입력 열의 여러 줄 머리글이 잘리지 않도록 헤더 높이를 확대했습니다.

## 1.8.4

- 체크박스로 선택한 기존 구간을 유지해 여러 양방향 구간을 동시에 선택할 수 있도록 수정했습니다.
- 1920×1080 전체화면에서 양방향 구간 10개가 보이도록 입력표 행과 머리글 높이를 추가로 최적화했습니다.
- `상세` 버튼의 높이와 내부 여백을 행 안에 맞춰 위아래 버튼이 겹치지 않도록 수정했습니다.

## 1.8.3

- 비교표에서 서비스수준이 변하지 않아도 `C→C`처럼 비교 전·후 등급을 표시합니다.
- `구간 연결 삽도`를 `구간 연결 관계도`로 명확하게 바꾸었습니다.
- Excel 연결 오류 확인 창은 입력 화면에서 분석 결과로 처음 넘어갈 때만 표시됩니다.
- 1920×1080 전체화면에서 입력·분석 결과표의 양방향 구간 10개를 한눈에 확인하도록 행·헤더·여백을 최적화했습니다.
- `Ctrl++`, `Ctrl+C`, `Ctrl+V`, `Ctrl+Delete`로 구간 추가·복사·붙여넣기·삭제를 실행하고 버튼에도 단축키를 표시합니다.
- Windows에서 `.ara1` 파일을 더블클릭하면 현재 버전의 프로그램으로 바로 열리도록 사용자별 파일 연결을 자동 갱신합니다.

## 1.8.2

- 연결된 교통량·PHF 입력칸의 Excel 버튼을 누르는 순간 기존 연결이 수기 입력으로 오인되어 해제되던 문제를 수정했습니다.
- 새 셀 선택이 확정되기 전에는 기존 값과 셀 주소를 유지하며, Excel 창을 닫거나 Esc로 취소해도 원래 연결정보를 보존합니다.

## 1.8.1

- 보조 입력 맨 끝에 `제한속도(km/h)(참고)` 열을 추가하고 프로젝트 저장·불러오기를 지원합니다.
- 교차로 분석 정리 Excel을 선택하면 상황·연도와 도착교차로를 대조해 `교차로 서비스수준(참고)` 값을 일괄 입력합니다.
- 교차로 정리표의 비교 열과 빈 LOS 열은 제외하고 실제 상황별 A~FFF 결과만 가져옵니다.

## 1.8.0

- 한 도로 그룹의 서비스수준이 모두 같으면 `“C”~“C”` 대신 `“C”`로 간결하게 표시합니다.
- 결과표의 교대 배경색을 개별 행이 아닌 양방향 구간 단위로 적용해 병합 셀과 방향별 셀이 체크무늬처럼 보이던 문제를 수정했습니다.
- 비교 문구의 변화 구간을 `가로명(출발교차로→도착교차로)는 “C”→“B”` 형식으로 표시합니다.
- 일부 구간만 서비스수준이 변한 경우 변화 구간을 먼저 제시하고, 나머지 동일 구간의 서비스수준 범위를 별도 문장으로 이어 표시합니다.

## 1.7.10

- 결과표를 외부도로·내부도로, 가로명, 양방향 구간, 방향 순서로 일관되게 묶어 표시합니다.
- 변화표의 양수 앞 `+`를 제거하고 변화가 정확히 0인 교통량·통행속도·서비스수준은 `-`로 표시합니다.
- 시행·개선 결과 문구를 외부도로와 내부도로로 분리하고, 비교 문구는 양쪽에 모두 존재하는 동일 구간만 대상으로 작성합니다.
- 모든 동일 구간의 서비스수준이 실제로 같을 때만 `동일하게 분석` 문구를 사용하며, 하나라도 바뀌면 변화한 구간명과 `D→C` 형식을 표시합니다. 신규 구간은 비교 문구에서 제외합니다.
- 연결 삽도에 한눈에 보기, 단계별 확대·축소, 100% 초기화, 확대율 표시, 마우스 휠 확대·축소와 좌클릭 끌기 이동을 추가했습니다.
- 비교표의 `검토`·`확인 필요` 버튼 글자가 잘리지 않고 펼친 뒤 원래 문구로 복원되도록 수정했습니다.

## 1.7.9

- 교차로 번호순 정다각형 배치 대신 가로 링크의 교차와 노드 관통을 우선 제거하는 자동 배치 탐색을 적용했습니다.
- 원형 배치로 해결되지 않는 가로망은 넓은 2차원 배치를 추가 탐색하도록 했습니다.
- 교차로 수에 따라 삽도 캔버스를 확대하고 가로·세로 스크롤로 전체 구간을 확인할 수 있도록 변경했습니다.

## 1.7.8

- 세로 스크롤 시 일반 입력 영역은 행 단위, 고정 열 영역은 픽셀 단위로 움직여 행 위치가 어긋나던 문제를 해결했습니다.
- 양쪽 입력 영역의 세로 스크롤을 동일한 픽셀 단위로 통일하여 모든 위치에서 행 경계를 일치시켰습니다.

## 1.7.7

- 삽도의 방향 화살표를 선 전체 길이 대신 구간 중앙의 동일한 짧은 크기로 표시하도록 변경했습니다.
- 구간명과 방향별 교통량·속도·서비스수준 문구가 겹치면 선 방향과 수직 방향으로 자동 이동하도록 개선했습니다.
- 삽도와 입력표 선택 연동이 현재 상황·연도 탭의 구간만 대상으로 하는지 회귀 검증을 추가했습니다.

## 1.7.6

- 오른쪽 입력 셀을 선택할 때 고정된 선택·가로명·교차로 열이 내부적으로 수평 이동하던 문제를 해결했습니다.
- 고정 열 전용 표의 수평 위치를 항상 첫 열에 고정하여 가로 스크롤 중에도 왼쪽 입력정보가 유지되도록 했습니다.

## 1.7.5

- 공통 헤더 스타일이 보조입력 열의 개별 색을 덮어쓰던 문제를 해결하고, 보조입력 헤더를 열별 색상으로 직접 그리도록 변경했습니다.
- 보조입력 본문색도 파랑·초록·노랑·보라가 실제 화면에서 구분되는 농도로 강화했습니다.

## 1.7.4

- 보고서 구간길이·보고서 교통량·교차로 LOS·접근로 LOS 보조입력 열을 서로 다른 옅은 색으로 구분했습니다.
- 단계 표시줄과 각 분석 페이지 사이에 노출되던 긴 회색 여백을 제거했습니다.
- 추가 탭 폭과 아이콘의 광학 중심, 탭 문구의 실제 글자 중심선을 다시 맞췄습니다.

## 1.7.3

- 분석연도·외부/내부도로·분석결과·세부결과·지침·사용설명서 등 프로그램 전체 탭을 선택된 콘텐츠와 이어지는 시트형 구조로 통일했습니다.
- 상위 탭과 내부 탭의 배경 명도를 구분하여 현재 선택된 시트와 그 안의 콘텐츠 범위를 한눈에 알아볼 수 있게 했습니다.
- 분석 탭의 닫기 `×`와 추가 `+`를 동일한 캔버스·선 굵기·표시 크기의 SVG 아이콘으로 교체하고, `+`를 탭 정중앙에 배치했습니다.

## 1.7.2

- 교차로명 열을 약 1.2배 넓히고 고정 교차로 정보와 구간 입력값 사이에 명확한 세로 구분선을 추가했습니다.
- 제목 영역은 흰색, 분석 탭 이하 영역은 연회색으로 구분하고 추가 탭의 `+`를 중앙에 배치했습니다.
- Excel 원본 선택·열기·적용·새로고침·주소 입력·경로 변경에 Excel 초록색과 전용 아이콘 체계를 적용했습니다.
- 프로그램이 새로 실행한 Excel은 셀 연결 완료 후 완전히 종료하고, 사용자가 이미 실행 중이던 Excel과 다른 통합문서는 유지하도록 수정했습니다.
- 보조 입력 네 열의 색상을 하나의 옅은 색으로 통일하고 도움말 메뉴에 내장 사용설명서를 추가했습니다.

## 1.7.1

- Excel 셀 연결 중 Enter를 눌러도 선택 셀이 아래로 이동하지 않도록 하고, 사용자가 Excel 창을 직접 닫아도 연결 대기 상태에서 즉시 정상 복귀하도록 개선했습니다.
- Excel 원본이 없는 상태에서 셀 연결 버튼을 누르면 현재 분석 탭의 원본 선택 과정임을 명확히 안내하도록 개선했습니다.
- 고정 열 경계의 글자 잘림을 보정하고, 분석 탭의 닫기·추가 버튼 크기와 탭 구분 디자인을 통일했습니다.
- 화면 글꼴, 여백, 행 높이와 주요 창 크기를 약 90%로 압축해 한 화면에 더 많은 정보를 표시하도록 조정했습니다.

## 1.7.0

- 교통량과 PHF를 탭별 Excel 원본의 셀·범위에 연결하는 새 작업 흐름을 추가했습니다.
- 복수 셀 교통량 합계, 숨김 셀 제외, 개별 반올림, 주소식 직접 입력을 지원합니다.
- 같은 도착교차로의 PHF 연결정보와 주기를 공유하고 녹색시간은 방향별로 유지합니다.
- 연결 오류의 마지막 정상값 유지, 입력표·분석결과 경고와 영구 변경 기록 패널을 추가했습니다.
- Ctrl+Z 되돌리기와 Ctrl+Y/Ctrl+Shift+Z 다시 실행, 분석 탭 메모를 추가했습니다.
- 기존 Excel 통합문서가 열려 있으면 새 보기 창을 사용하고 듀얼 모니터의 반대편 화면에서 연결합니다.

## 1.6.3

- 구간 연결 삽도의 방향별 얇은 화살표에 교통량, 평균통행속도, 서비스수준을 한 줄로 표시하도록 추가했습니다.
- 양방향 구간은 두 방향의 화살표와 분석값을 각각 분리해 표시합니다.

## 1.6.2

- 표 안의 입력용 드롭다운 화살표 폭과 내부 여백을 줄여 좁은 셀에서도 입력 중인 글자가 더 길게 보이도록 개선했습니다.

## 1.6.1

- Excel 원본 선택과 프로젝트 열기·저장 창을 Windows 기본 파일 탐색기 형식으로 변경해 주소창 경로 붙여넣기를 지원했습니다.
- 최근 선택한 Excel 파일 위치를 다시 여는 동작은 그대로 유지했습니다.

## 1.6.0

- 보조 입력 열의 긴 머리글이 위아래로 잘리지 않도록 높이와 글자 크기 조정
- 지침·공식 페이지 탭 아래의 불필요한 회색 구분선 제거
- 버튼 글자를 원해상도로 고정하고 즉시 눌림·탄성 복귀·호버 광륜 모션 강화
- `.xlsx`·`.xlsm` 저장값과 시트 목록을 Excel 별도 실행 없이 빠르게 읽도록 개선
- 교통량 셀의 `=` 키 연결, 편집 가능한 `fx` 연결식 입력줄, 연결식 복사·붙여넣기 지원
- `Ctrl+H`에서 선택 연결의 파일·시트를 한 창에서 바꾸고 즉시 새로고침

## 1.5.1

- 자동 업데이트 후 새 실행파일이 이전 PyInstaller 임시 폴더를 참조하던 문제 수정
- 원래 설치 경로와 다운로드 폴백 경로 모두 독립 실행환경으로 안전하게 재시작

## 1.5.0

- 입체화검토 프로그램과 버튼·카드·표·입력요소의 디자인 토큰 통일
- 버튼 눌림 중 투명도 효과를 제거하여 글자 뭉개짐 수정
- 버튼 배경만 축소하고 글자는 원래 해상도로 그리는 선명한 스프링 모션 적용

## 1.4.1

- 새 버전 EXE 자동 다운로드와 진행률 표시 추가
- GitHub SHA-256 검증 후 기존 실행파일 교체 및 자동 재실행 지원
- 업데이트 재시작 전에 저장되지 않은 프로젝트 확인 절차 적용

## 1.4.0

- 버튼 클릭·키보드 실행에 짧고 분명한 눌림 및 복원 애니메이션 적용
- 분석 화면과 탭 전환에 부드러운 페이드 모션 적용
- 팝업 창 표시 애니메이션과 모션 줄이기 환경설정 지원

## 1.3.2

- GitHub 릴리스 자산명을 영문으로 통일하여 한글 파일명 인코딩 오류 수정

## 1.3.1

- GitHub Actions가 설정된 Python을 사용하도록 Windows 빌드 스크립트 수정
- 넓은 도로와 흰색 배경을 적용한 새 아이콘 반영

## 1.3.0

- GitHub Releases 자동 업데이트 확인 기능 추가
- 입력표와 구간 연결 삽도의 양방향 선택·강조 기능 추가
- 독립 GitHub 저장소 및 Windows 자동 배포 구성 추가
